import * as React from 'react';
declare type Props = {
    isVisible: boolean;
    children: React.ReactNode;
    enabled: boolean;
    style?: any;
};
export default function ResourceSavingScene({ isVisible, children, style, ...rest }: Props): JSX.Element;
export {};
