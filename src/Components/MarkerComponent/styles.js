import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
  textName: {
    fontSize: 12,
 
    fontWeight: "bold"
  },
  infoText: {
    fontSize: 8,

    marginTop: 8,
  }
});

export default styles;
